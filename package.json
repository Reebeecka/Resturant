{
  "dependencies": {
    "axios": "^0.26.1",
    "react-transition-group": "^4.4.2"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
